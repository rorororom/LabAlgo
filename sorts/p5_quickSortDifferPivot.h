#ifndef QDS
#define QDS
#include <stdlib.h>

void sortCenterElement      (int* arr, size_t n);
void sortMedianOfThree      (int* arr, size_t n);
void sortRandomElement      (int* arr, size_t n);
void sortMedianOfThreeRandom(int* arr, size_t n);
#endif
